# 模块名称

> 负责人：姓名  
> 状态：设计中 / 代码实现中 / 已实现并验证  
> 最后更新：YYYY-MM-DD  
> 代码版本：Git commit 或 tag

## 模块简介

用不超过 200 字说明：

- 这个模块解决什么问题；
- 为什么需要它；
- 它在完整流程中的位置。

## 输入

列出输入文件、字段、形状、数据类型、单位、取值方向和缺失值约定。

## 输出

列出输出文件、字段、形状、分数方向、保存目录和版本信息。

## Python 接口

```python
def public_function(...):
    """说明参数、返回值和异常。"""
```

只记录允许其他模块调用的稳定接口，不要把内部辅助函数全部暴露出来。

## 命令行接口

```bash
command-name --required-argument value
```

列出参数默认值和一条最小可运行命令。

## 调用示例

提供一个最小 Python 示例和预期输出。

## 配置

列出配置文件位置、关键参数、默认值和随机种子。

## 依赖

列出 Python、CUDA、模型权重、第三方包和最低硬件要求。

## 错误与边界

说明：

- 缺失输入如何处理；
- 哪些情况直接报错；
- 已知不支持的情况；
- 数值方向和单位；
- 防止数据泄漏的限制。

## 测试与实际结果

```bash
pytest ...
```

记录真实结果，而不是写“应该通过”。

## 产物

列出模型权重、配置、日志、预测、报告和内容哈希。

## 变更记录

| 日期 | 版本 | 修改 | 作者 |
|---|---|---|---|
| YYYY-MM-DD | v0.1 | 初始实现 | 姓名 |

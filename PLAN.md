# 详细实施计划

## 总目标

输入给定抗原（优先包含序列、结构和目标表位），生成一组多样化的抗体或纳米抗体候选，并以结合/中和潜力、结构可信度、天然性、可开发性和多样性进行多目标排序。所有训练、推理和评价过程必须可复现。

## 阶段0：任务冻结与数据审计（第1周）

交付物：数据清单、统一数据字典、风险清单。

- 核对赛事正式输入、输出、测试方式和提交文件格式；
- 记录83个CSV的来源、靶点、实验类型、单位、标签方向和样本量；
- 检查重链/轻链/抗原序列合法性、重复项、缺失值和异常值；
- 区分真实实验标签、计算预测标签和二分类标签；
- 建立数据许可证和论文来源表；
- 原始数据只读，不直接修改。

验收标准：每条进入训练的数据都可追溯到原文件，且“越大越好”的统一分数方向正确。

## 阶段1：可靠Benchmark（第1～2周）

交付物：本仓库已实现的预处理、拆分、基线训练和评价代码。

- 来源内将测量值转为百分位，避免KD、IC50和EC50量纲混用；
- 主拆分采用整来源留出；补充同靶点序列簇拆分；
- 建立零规则、k-mer线性模型、预训练蛋白语言模型三个层级基线；
- 评价Spearman、Pearson、RMSE、MAE和Top-k富集；
- 每个来源单独报告并进行宏平均；
- 重复至少3个随机种子，报告均值与标准差。

晋级标准：预训练表示相对k-mer基线在宏平均Spearman和Top-k富集上稳定提升，且不存在数据泄漏。

## 阶段2：抗原条件亲和力排序（第2～4周）

交付物：抗体—抗原双塔或交叉注意力排序器。

- 抗体编码：重链、轻链、CDR分别编码；
- 抗原编码：有序列时使用蛋白语言模型，有结构时补充表面/表位特征；
- 训练目标：来源内pairwise ranking loss + 回归损失；
- 对缺失抗原序列的数据使用靶点ID嵌入，但不把它作为跨靶点泛化证据；
- 对AbRank和含抗原序列的数据做严格抗原簇外测试；
- 做消融：去掉抗原、去掉轻链、去掉结构、去掉排序损失。

晋级标准：加入抗原后在抗原簇外测试上显著优于仅抗体模型。

## 阶段3：候选生成（第4～6周）

交付物：至少两种生成器及统一候选格式。

- 基线生成器：本仓库的CDR-H3局部突变，用于流程测试；
- 序列生成器：IgLM或其他抗体语言模型，生成不同长度CDR；
- 结构条件生成器：IgGM、dyMEAN或RFantibody，针对表位生成序列和结构；
- 每个靶点先生成1万～10万条廉价候选；
- 去重、限制与已知序列相似度，并保留不同CDR长度和序列簇。

验收标准：固定种子可重复生成；输出包含序列、生成模型、随机种子、条件和初始分数。

## 阶段4：分层筛选与多目标优化（第5～7周）

交付物：候选漏斗和Pareto前沿。

筛选顺序从便宜到昂贵：

1. 序列合法性、重复和明显可开发性风险；
2. 语言模型天然性和亲和力排序器；
3. AntiFold序列—结构一致性；
4. 抗体结构预测与置信度；
5. 抗体—抗原复合物预测、界面PAE/DockQ类指标；
6. 能量或亲和力变化预测；
7. 多样性聚类，每簇保留代表序列。

不要把所有分数简单相加。先设硬性安全/结构阈值，再在亲和力、天然性、可开发性和多样性上做Pareto选择。

## 阶段5：验证、消融和错误分析（第7～8周）

交付物：完整结果表、统计检验、失败案例。

- 与IgLM、AntiFold、dyMEAN/IgGM等公开基线公平比较；
- 统计置信区间，并按靶点、CDR长度、序列新颖度分层；
- 检查训练集近邻，排除“记住答案”；
- 展示抗原信息、结构筛选、可开发性筛选各自贡献；
- 人工检查高分错误和低分正确案例；
- 明确计算结果不等于实验中和结论。

## 阶段6：复现与提交（贯穿全程，第8周冻结）

交付物：代码、权重、Docker/Conda环境、文档、示例和抗体序列。

- 固定数据版本、Git提交、依赖版本、随机种子；
- 一条命令完成预处理、训练、评价和推理；
- 保存训练曲线、最优检查点、配置和硬件日志；
- 在全新云实例上从零复现；
- 准备一个小型演示数据，10～20分钟内跑通；
- 最终候选记录全部筛选分数和淘汰原因。

## 计算资源建议

- 当前k-mer基线：CPU、16 GB内存即可；
- 抗体/蛋白语言模型微调：单卡24～48 GB显存起步；
- 结构模型推理：按候选规模使用多卡并行；
- 全量INDI2解压约需额外数十GB空间，应在云端按分卷流式处理；
- SAbDab结构包解压约30.5 GB，建议至少预留100 GB工作空间。


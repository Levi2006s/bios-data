"""Mathematical ranking components for the Bio-OS benchmark.

Public APIs live in ``preferences``, ``math_ranker``, ``evaluation`` and
``ensemble``.  The package intentionally avoids eager imports so every module
can also be executed safely with ``python -m``.
"""

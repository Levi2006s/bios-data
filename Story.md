# AbCompass：把抗体候选排成一条可信的实验路线

## 一句话介绍

**AbCompass（抗体罗盘）** 是我们的抗原条件抗体亲和力排序 AI。给它一个抗原和一组候选抗体，它不会编造“已经验证的亲和力”，而是综合抗体序列、抗原序列、实验标签质量和模型不确定性，给候选排出实验优先级。

名字里的 Compass 强调我们的定位：AI 负责指方向、缩小实验搜索空间，最终结论仍由 ELISA、SPR 等实验给出。

## 为什么我们做的是排序，而不是假装预测一个完美的 KD

初赛的直接任务是比较模型排名与真实亲和力排名的一致性，主指标是 Spearman correlation。不同公开数据里的 KD、IC50、EC50、荧光信号和作者自定义 fitness 量纲互不兼容，直接混在一起回归会得到一个没有统一物理意义的数字。

AbCompass 先在每个可比较实验组内部统一“越大越好”的方向，再把标签转换为相对百分位。模型学习的是：在同一实验条件下，哪个候选更值得优先测试。

## 我们现在能做到什么

AbCompass 已经形成从原始数据到候选排名的完整链路：

1. 审计 83 个异构 CSV，共约 460 万行原始记录。
2. 区分连续亲和力标签、二元标签、弱标签和无标签数据。
3. 统一 KD、IC50、EC50 等指标方向，并保留来源、单位、质量等级和标签可信度。
4. 识别精确重复、跨研究重复和训练—验证泄漏。
5. 构建 exact-identity-safe 和严格框架家族隔离的训练、验证、测试 fold。
6. 训练 pointwise、pairwise、实验条件专家、稀疏交互和 GPU 神经模型。
7. 对齐多个模型的候选 ID，在百分位秩空间做确定性集成。
8. 输出候选分数、Rank、模型来源和验证指标，支持复现实验。
9. 按队友标签认证表，对 1,352,114 条整理记录完成 A/B/C/D 与多任务路由，避免把 KD、EC50、IC50、binding、ADCC 和 OVA 混成一个标签。

如果输入缺少合法抗原序列，系统会走低置信度回退模型，而不是把“没有抗原”伪装成正常的抗原条件预测。

## 核心算法

AbCompass 当前是一个分层路由的排序系统。

```text
抗原序列 + 候选抗体重链/轻链
        │
        ├── 数据质量与序列合法性检查
        │
        ├── 有合法抗原 ──> GPU 抗体–抗原双塔神经模型 × 2
        │                         │
        │                         └── 百分位秩集成 ──> 主排名
        │
        └── 缺失抗原 ──> 稀疏 k-mer / 交互特征模型 ──> 低置信度回退排名
```

### 1. 数据与标签层

每条训练记录都带有来源、metric、label origin、tier 和 label quality。阶段13进一步加入 A/B/C/D 可信等级：A 级 KD 主监督权重 1.0，B 级 AlphaSeq 权重 0.6，C 级 EC50/IC50 进入独立辅助任务，D 级 binding/OVA/ADCC 不进入亲和力主头。等级控制采样和损失权重，不会被当成预测标签。

正式 v3 路线将每个来源内方向校正后的标签转换为 0–1 相对排序目标。标签认证审计进一步发现 AbRank 内部仍混合 KD、IC50 和多个数据来源；实验性 v4 已改为只在 `Source × endpoint × antigen` 可比较组内定义偏好，不再假设跨终点分数可比。

### 2. 严格验证层

我们不仅检查完全相同的重链+轻链是否跨 fold，还建立框架家族压力测试。家族键综合重链/轻链框架锚点、长度、CDR-H3 长度和尾部稀疏签名；整个家族只能进入一个 fold。

这个划分比普通随机切分困难得多。它不是正式 MMseqs2 聚类，但能快速暴露“模型只记住相近突变体”的问题。

### 3. 稀疏数学基线

基础模型使用抗体和抗原的字符 k-mer、长度、疏水性、电荷、半胱氨酸等特征，并用 Huber + elastic-net SGD 拟合排序百分位。

升级版加入显式抗体 3-mer × 抗原 3-mer 哈希交叉项。这个简单改动使严格家族 Spearman 从 0.1313 提升到 0.2102，证明抗体–抗原交互是有效信号。

### 4. GPU 抗体–抗原双塔模型

主模型分别编码：

- 抗体重链；
- 抗体轻链；
- 抗原序列。

每条链先经过可学习的氨基酸 embedding、多卷积核 CNN 和全局池化。随后构造：

- 抗体表示；
- 抗原表示；
- 抗体与抗原表示的绝对差；
- 抗体与抗原表示的逐维乘积。

这些交互特征进入 MLP 排序头。训练使用 Smooth-L1 loss、数据集温度采样、梯度裁剪和严格家族 validation Spearman 选模。pairwise 排序对只允许来自同一 comparison group，避免跨论文、跨 assay 比较不可比标签。

阶段13增加两阶段迁移：109.96 万条缺抗原的 AlphaSeq 记录只用于抗体侧组内排序辅助预训练；随后只用具有真实抗原序列的记录微调双序列模型。这样利用大数据，同时不把空抗原伪装成真实输入。

### 5. 多模型秩集成

两个模型使用相同的数据采样，但采用不同随机初始化。原始模型分数不可直接平均时，先转换成全局百分位秩，再做 50/50 平均。这样保留两个模型都同意的顺序，并减少单次初始化噪声。

## 当前指标

> 2026-08-04 复核：旧 `framework_family_split` 的 AlphaSeq train/validation 间发现 39,272 个完全相同抗体序列。下表 0.6683 保留为旧 fold 的历史模型开发指标，不再称为“全局零重复泛化分”。阶段14建立 `global_family_v2_split` 后，三个 fold 的精确序列交集均为 0；在全部 718,881/162,291 条 AlphaSeq train/validation 上，CNN + ESM150 + 位置模型达到严格 Spearman **0.3565**。这两个分数口径不同，不能直接比较。

> 阶段15进一步确认 Li affinity1 与 Engelhart 是 87,807 个唯一抗体及标签完全一致的镜像数据。按生物学唯一记录去重后，assay 专家与阶段14融合在 94,848 条严格验证记录上达到 Spearman **0.4378**、Top-10 enrichment **4.1507**。双链 ESM150 达到 0.3952，较遗漏轻链的单链版本提高 0.0127，但融合增益不足以晋升。

> 阶段16加入 assay 条件化重轻链位置卷积，并将 pairwise 限定在同一 comparison group。两个独立 seed 分别达到 0.4519 和 0.4530；两 seed 与阶段15粗粒度秩融合达到严格 Spearman **0.4603**、Pearson **0.4824**、Top-10 enrichment **4.2319**。相对阶段15提高 0.0225，当前瓶颈集中在 landscape1（0.2736），而 landscape2 已达到 0.6553。

> 阶段17确认 landscape1 混合三类主要重轻链长度/母本，validation 同序列均值上限约 0.699；landscape2 基本为单一母本，上限约 0.876。重复测量聚合、置信加权和长度母本代理融合将严格 Spearman 小幅提高到 **0.4614**。下一步不再用长度近似母本，而是在 train-only ESM 空间识别真实母本并学习残基级母本差分。

> 阶段18已完成 train-only ESM-2 150M 母本聚类：在 assay 与重轻链长度组内只用训练序列拟合 32 个簇，验证序列仅做最近中心分配。母本簇位置卷积单模 Spearman 为 0.4525，但与阶段17模型具有互补性；22.5% 秩融合把严格 Spearman 提高到 **0.4622**。H119/L115 随机初始化专属模型仅得 0.1245，已拒绝，证明下一步需要共享骨干迁移适配而不是完全拆分训练。

> 阶段19把阶段18全局最佳检查点迁移到 H119/L115，并冻结序列 embedding 与卷积骨干。专家首轮达到 0.1632，较随机初始化显著提高，但继续适配会遗忘且仍低于全局模型同组的 0.1657，因此未晋升。当前结论是保留共享模型，下一步只允许受约束残差/OOD 门控，并用训练内部二级 fold 选择强度。

> 阶段20加入完全由训练 ESM 空间定义的连续 OOD 百分位。H119/L115 有 30.45% 验证唯一序列超过其训练簇距离第90百分位，显著高于其他组的 7.86%–17.77%；远域桶 Spearman 仅约 0.216，证明它是可靠的不确定性指标。但 OOD 升/降权都没有超过固定融合，因此它只用于候选风险标记，不用于制造验证集上的虚假提分。

> 阶段21建立只来自外层训练集的二级 family holdout，并用它选择“绝对序列 + 相对共识母本突变对”的双通道模型。内层固定出 32.5% 差分权重后，外层不再调权，严格 Spearman 从 0.46221 提升到 **0.46252**，Pearson 0.48014，Top-10 enrichment 4.2276。按 22,513 个精确抗体对配对 bootstrap，新模型胜率 97.2%，但95%区间下界略低于0，因此这是谨慎晋升的小幅可信增益。

所有下表结果来自严格框架家族 validation，而不是容易的数据随机切分。

| 模型 | Spearman | Pearson | Top-10 enrichment |
| --- | ---: | ---: | ---: |
| k-mer 加性基线 | 0.1313 | 0.3444 | 1.4994 |
| 稀疏抗体–抗原交互 | 0.2102 | 0.4142 | 0.5363 |
| GPU 双塔，seed 20260803 | 0.3637 | 0.4985 | 1.4942 |
| GPU 双塔，seed 20260804 | 0.3512 | 0.4939 | 1.3616 |
| **双模型百分位秩集成** | **0.3780** | 0.4525 | **1.7472** |
| **冻结 ESM-2 交互排序头** | **0.4456** | **0.5268** | **2.4340** |
| **ESM 65% + CNN 35% 秩集成** | **0.4571** | **0.5137** | **2.6750** |
| **ESM-8M + ESM-35M + CNN 高分候选** | **0.4607** | **0.5200** | **2.7835** |
| 全量 CNN + pairwise rank | 0.5556 | 0.6192 | 3.6727 |
| ESM2-150M + 同抗原条件排序 | 0.6161 | 0.6819 | 4.1488 |
| **AlphaSeq 位置专家 + ESM150 + CNN 域路由集成** | **0.6607** | **0.6781** | **4.7179** |
| **母本簇位置互作 + ESM 残基层 + CNN 集成** | **0.6683** | **0.6826** | **4.8754** |

GPU 模型训练使用一张 NVIDIA GeForce RTX 4090（24,564 MiB）。模型在第 5–6 epoch 达到最佳 Spearman；继续降低训练损失并没有改善验证排序，因此我们保存验证最优检查点，而不是盲目选择最后一轮。

## 我们强在哪里

### 强项一：不让数据泄漏替模型刷分

数据中存在大量重复序列和跨论文复用记录。我们明确检测并隔离精确身份与框架家族，严格验证分数虽然更低，却更接近面对新候选时的真实风险。

### 强项二：模型真的使用了抗原

普通线性特征只能学习“这个抗体通常怎样”和“这个抗原通常怎样”。AbCompass 加入显式交叉特征，并进一步使用神经双塔的差值和乘积表达抗体–抗原匹配。严格 Spearman 从 0.1313 提升到 0.3780，是目前最重要的模型进步。

### 强项三：知道 pairwise 应该在哪里使用

早期跨终点 pairwise 模型在严格家族外验证中表现不稳定，因此未进入正式集成。认证表驱动的 v4 改为只在同 assay、同抗原组内构造偏好对：KD-only 专家把 9 个可评估 KD 组的 macro Spearman 从 0.1859 提升到 0.3133，但最大 AlphaSeq 组下降且置信区间仍跨 0。因此它被保留为跨来源复核专家，而不是无条件替换赛事模型。

### 强项四：每个结论都能复现

数据处理、fold、随机种子、模型配置、逐 epoch 指标、预测文件和测试全部保存。当前代码有 70 项自动化测试，阶段报告统一收录在 `docs/stage0` 到 `docs/stage21`。

### 强项五：有明确的回退和风险边界

有抗原、无抗原、弱标签和家族外候选不会被当成同一种情况。系统可以给出排名，同时告诉队友这个排名来自主模型还是低置信度回退。

## 我们还不能声称什么

AbCompass 的输出是计算排序，不是湿实验结果。当前不能保证：

- 某个候选一定具有更低的真实 KD；
- 候选一定能够表达、纯化或稳定存在；
- 候选一定具有中和活性或目标特异性；
- 模型能够可靠处理完全新颖的抗原、表位或抗体结构；
- 当前分数足以保证进入初赛前 20。

预训练 ESM-2 已进入主模型；三维复合物结构和表位几何尚未纳入正式模型。这些仍是下一步，而不是已经完成的能力。

## 给队友的结论

如果现在必须提交，AbCompass 已经可以生成一份真实可运行、可复现的保底排名。当前推荐配置是：

1. 有合法抗原序列且按旧赛事口径提交时，高分候选使用 50% ESM-8M + 20% ESM-35M + 15%/15% 两个 CNN；阶段 6 的 65% ESM-8M + 17.5%/17.5% CNN 保留为稳定基线。
2. 缺失抗原时，使用稀疏线性交互回退，并标记低置信度。
3. 对明确的 KD 组可附加 KD-only pairwise 专家复核，但不与 IC50/OVA 混成一个分数。
4. 所有最终 Rank 使用确定性 tie handling。

预训练 ESM-2 已在同一个冻结的严格家族 fold 上完成公平比较。阶段12把母本簇位置互作、ESM150 和 CNN 域路由集成提升到严格 Spearman 0.6683。阶段13已在 RTX 4090 上完成 AlphaSeq 抗体侧预训练和真实抗原迁移微调，新 CNN 单模型达到 0.4856；与阶段12融合的同集提升仅 0.00018，因调权过拟合风险而不晋升。当前最大瓶颈仍是 AlphaSeq 家族外泛化、抗原序列缺失和编码器容量，下一步把可信路由迁移到 ESM150/抗体专用编码器。

# 序列匹配排序竞赛初赛技术方案

## 1. 方案摘要
本方案针对异构数据集下的序列对排序任务设计，核心目标是基于给定的靶标与候选序列，输出精准的排序结果，以Spearman秩相关系数为核心评价指标。

官方提供的数据集具有强异构性：来自数十项独立研究，标签类型、测量尺度、数值方向差异显著，存在大量同源重复样本、截断标签与特征缺失。直接拼接训练会导致严重的验证虚高与泛化能力下降。

本方案不从头训练大模型，也不直接照搬单一公开基线，核心路线为：
1. 逐文件完成标签语义注册与跨数据集去重
2. 按数据质量分层，差异化参与训练
3. 基于预训练序列语言模型构建三路交互架构
4. 采用排序学习框架优化相对顺序，而非绝对数值
5. 引入多专家输出头适配不同实验类型
6. 利用少量三维结构标注做知识蒸馏，提升纯序列模型性能
7. 严格按研究单元、同源簇划分数据集，杜绝泄漏
8. 多折多模型百分位集成，提升最终鲁棒性

方案核心优势在于针对异构数据的精细化治理与无泄漏验证体系，而非单纯扩大参数量。

## 2. 赛题目标与交付要求
### 2.1 任务定义
给定靶标序列与一组候选序列，模型为每个候选生成匹配活性得分，按得分降序生成连续整数排名。候选分为两类：
- 常规样本：包含主、辅两条子序列，需同时输入
- 单域样本：仅包含主序列，辅序列留空

最终提交格式为「主序列-辅序列-排名」三元组。

### 2.2 评价指标
核心指标为Spearman秩相关系数，仅衡量相对排序的一致性，不受绝对值单调变换的影响。因此模型优化目标聚焦排序正确性，而非跨实验体系的绝对数值回归精度。

### 2.3 工程交付要求
竞赛需提交完整可复现产物：
1. 算法设计文档与训练技术说明
2. 规范注释的源代码
3. 完整依赖版本清单与环境配置步骤
4. 训练、优化、推理的运行命令与示例
5. 最终模型权重文件
6. 端到端可运行的演示案例
7. 可在指定环境复现的输出结果

模型精度与工程可复现性为并列核心目标。

## 3. 官方数据审计结果
### 3.1 序列数据概况
- 总量：83个CSV文件，约460万行样本，对应22个独立研究来源
- 标签类型：连续定量值、半定量排序值、二分类标签、模型预测伪标签等多种类型
- 核心问题：
  - 标签方向不统一：部分数据集数值越大性能越好，部分则相反
  - 同源重复严重：多个数据集来自同一母本研究，样本高度重叠
  - 格式异常：个别文件表头错位，需特殊解析逻辑
  - 截断效应：大量样本处于检测上下限，存在并列标签
  - 特征缺失：部分样本缺少靶标序列，仅包含候选自身信息

二分类辅助数据约65万行，可用于对比学习与领域适配，不直接进入主排序监督。

### 3.2 结构标注数据
- 元数据约2万条，对应1万余个唯一三维结构样本
- 仅约700余个样本带实测活性标签，其余仅含结构信息
- 同一结构会被展开为多行样本，直接按行划分会导致严重泄漏
- 原始数据包含三套不同标注规范的文件，全量解压占用约30GB存储空间

### 3.3 单域序列数据
- 标注数据集约3万条样本，其中千余条带完整活性标签与对应靶标
- 去重后有效监督样本进一步减少，且存在部分标签冲突
- 大规模无标注序列库总量约20GB，暂无直接监督标签，可用于无监督预训练

## 4. 数据治理方案
### 4.1 统一主表设计
构建标准化数据主表，每条样本包含核心字段：
- 样本唯一ID、数据集ID、研究来源ID、靶标ID
- 候选主序列、候选辅序列、靶标序列
- 样本类型、实验类型、标签语义
- 原始标签值、规范标签值、组内排名
- 标签质量等级、是否截断、重复组ID
- 同源簇ID、划分集合标记

哈希字段仅用于去重与划分，不输入模型。

### 4.2 数据质量分层
按标签可信度分为五层，差异化使用：

| 层级 | 定义 | 用途 |
|---|---|---|
| 金标 | 单位明确、测量可靠的定量实验数据 | 主排序任务核心监督 |
| 银标 | 半定量、替代终点实验数据 | 独立任务头，低权重参与训练 |
| 弱标 | 模型预测值、计算虚拟值 | 蒸馏监督、辅助训练 |
| 辅助 | 二分类标签、无标注序列 | 对比学习、正则约束 |
| 隔离 | 单位不明、异常值、标签冲突未解决 | 不进入训练流程 |

### 4.3 标签标准化流程
1. 逐文件人工审核标签语义，登记测量类型、单位、方向与实验方法
2. 仅金标数据统一数值尺度，银标及以下保留相对排序关系
3. 在「同一研究+同一靶标+同一实验方法」分组内生成百分位排名
4. 截断值与并列值不构造强制排序对，避免引入错误监督
5. 重复测量样本取中位数并标注离散度，按重复数调整采样权重
6. 弱标签数据采用动态采样平衡，避免淹没高质量实验数据

### 4.4 无泄漏数据集划分
构建三套独立划分策略，用于不同维度验证：
1. **同源簇划分**：按序列同源簇、重复组、结构ID分组，同一簇不跨集
2. **研究来源划分**：整项研究整体留出，验证跨研究泛化
3. **靶标冷启动划分**：整类靶标整体留出，验证零样本泛化

所有划分以连通分量为单位，确保没有任何形式的数据泄漏。随机按行划分仅作为泄漏幅度对照，不用于模型选型。

## 5. 模型设计
### 5.1 基础表征层
采用三路独立编码架构，保留序列独立语义，不在输入层直接拼接：
1. 候选主序列编码器：提取全长全局特征与关键功能区局部特征
2. 候选辅序列编码器：仅常规样本启用，单域样本通过掩码旁路
3. 靶标序列编码器：提取靶标全局特征，缺失时启用专用旁路

基础编码器采用公开预训练序列语言模型，冻结主干参数。

### 5.2 主干交互网络
以三流式门控交互架构为基线：
1. 主-辅序列融合门：建模候选内部的协同作用
2. 候选-靶标交叉注意力门：建模两者的匹配交互模式
3. 全局+局部双池化：整合全局匹配与关键区域匹配信号
4. 轻量全连接交互层：输出最终匹配表征

### 5.3 多专家输出头
针对不同实验类型的标签分布差异，设计实验感知的多专家输出头：
- 金标定量排序头：输出连续得分，用于排序损失优化
- 银标半定量排序头：独立参数，适配不同测量尺度
- 二分类分类头：处理辅助二分类数据
- 弱标签蒸馏头：适配伪标签监督

所有专家共享底层表征，通过门控根据实验类型动态加权。

### 5.4 损失函数设计
采用多任务加权损失，核心为排序学习损失：
- 主损失：金标数据的pairwise + listwise混合排序损失
- 辅助损失：银标排序损失、二分类损失、结构蒸馏损失、一致性正则
- 权重策略：按数据质量分层加权，金标权重最高

### 5.5 微调策略
采用渐进式微调，控制过拟合风险：
1. 第一阶段：完全冻结编码器，仅训练交互层与输出头
2. 第二阶段：对编码器顶层2-4层加入低秩适配（LoRA）小规模微调
3. 第三阶段：严格验证增益，无稳定提升则回退冻结版本
4. 所有微调均在三套严格划分上交叉验证

### 5.6 结构知识蒸馏
利用少量带标注三维结构数据构建教师模型，将结构信息蒸馏到纯序列学生模型：
1. 结构教师基于三维结构提取更精准的匹配表征
2. 按结构样本为单位构造蒸馏损失，同一结构只贡献一次监督
3. 学生模型仅使用序列信息，学习教师的排序偏好
4. 最终推理完全不依赖结构数据，保证通用性

### 5.7 检索增强与集成
1. **检索残差校正**：在训练集表征库中检索相似靶标与候选，对全局得分做局部残差校正，同时输出不确定性
2. **百分位排序集成**：不直接平均原始得分，先转换为折内百分位再加权平均，避免不同模型得分尺度差异的影响

## 6. 验证体系
### 6.1 多维度评价指标
每个实验同时报告以下指标：
- 全局Spearman相关系数
- 按研究来源宏平均Spearman
- 按靶标宏平均Spearman
- 留出研究来源Spearman（跨研究泛化）
- 冷启动靶标Spearman（零样本泛化）
- Bootstrap 95%置信区间
- 截断样本与非截断样本分层指标
- 常规样本与单域样本分层指标

### 6.2 负对照验证
设置多重负对照，验证模型学到真实规律而非捷径：
1. 标签打乱测试：Spearman应接近0
2. 纯元数据模型：仅用来源、长度等元数据应得分极低
3. 随机划分vs严格划分对比：量化数据泄漏带来的虚高幅度
4. 泄漏自动检查：训练、验证、测试集之间不得存在同源、重复样本

### 6.3 模型选型准则
内部模型选型综合评分：
- 50% 权重：宏平均Spearman
- 25% 权重：研究来源留出Spearman
- 25% 权重：靶标冷启动Spearman

优先保障泛化性能，而非全局拟合分数。

## 7. 创新性与技术价值
相对于直接复用公开基线，本方案核心改进点：
1. **异构数据精细化治理**：逐文件标签语义注册、方向统一、质量分层
2. **严格无泄漏验证体系**：基于同源连通分量的多维度划分
3. **截断感知排序学习**：针对检测上下限与并列标签优化监督构造
4. **实验感知多专家架构**：适配不同实验类型的标签分布
5. **双路径缺失特征适配**：针对靶标缺失、辅序列缺失设计专用门控
6. **结构到序列蒸馏**：用少量结构数据提升纯序列模型性能
7. **百分位排序集成**：解决多模型得分尺度不一致问题
8. **全流程工程化设计**：从数据到提交全链路可复现

## 8. 分阶段实施计划
### Phase 0：环境与仓库搭建
**目标**：完成开发环境配置、代码仓库初始化、原始数据归档
- 固定Python、深度学习框架、CUDA版本，构建可复现conda环境
- 建立代码目录规范，配置/数据/模型/日志/输出分离
- 原始数据只读挂载，建立版本管理
- 建立Git分支管理规范与提交准则

**验收**：环境测试通过，仓库正常推拉，基础命令可执行。

### Phase 1：数据审计与标准化
**目标**：完成全量数据解析、标签注册与标准化主表生成
- 建立83个文件的manifest清单，逐文件登记格式与标签语义
- 修复异常格式文件，统一数据读取逻辑
- 完成序列格式校验与异常值隔离
- 生成五层数据标签
- 输出标准化主表与数据质量报告

**验收**：每条样本可追溯来源，标签方向100%审核，异常有记录。

### Phase 2：去重与数据集划分
**目标**：完成全量去重与三套无泄漏划分
- 序列级哈希去重、同源簇聚类
- 跨数据集重复样本对齐与合并
- 构建同源连通分量图谱
- 生成三套固定划分manifest
- 开发数据泄漏自动检查工具

**验收**：集间无重复、无同源样本，泄漏检查零报警。

### Phase 3：基线模型跑通
**目标**：完成特征工程基线与冻结PLM基线，建立评估体系
- 实现k-mer+理化特征的LambdaRank快速基线
- 实现冻结预训练模型表征+排序头基线
- 复现公开三流式模型推理与微调流程
- 搭建统一评估脚本与看板
- 完成所有负对照实验

**验收**：端到端全链路跑通，三套划分都有完整评估报告。

### Phase 4：主模型优化
**目标**：完成核心模型开发，超越基线性能
- 实现三路交互主干网络
- 实现pairwise+listwise混合排序损失
- 实现多专家输出头与实验门控
- 实现缺失特征门控旁路
- 实现来源平衡采样器
- 完成核心模块消融实验

**验收**：严格划分下宏平均Spearman显著优于基线，多随机种子稳定。

### Phase 5：进阶优化与集成
**目标**：引入进阶技术，进一步提升泛化性能
- 实现编码器顶层LoRA微调
- 实现结构教师模型与知识蒸馏
- 实现检索增强残差校正
- 完成多折交叉验证与百分位集成
- 完成模型压缩与推理优化

**验收**：各模块在严格验证下有稳定增益，无增益模块不进入最终提交。

### Phase 6：最终交付整理
**目标**：完成所有交付物，通过最终dry run
- 生成最终提交文件，校验格式合规性
- 固化模型权重与配置
- 撰写算法文档与训练说明
- 完善代码注释与README
- 制作一键运行演示案例
- 从空环境完整复现全流程

**验收**：提交文件格式合规，空环境可复现，文档完整。

## 9. 服务器配置与工程约束
### 9.1 当前配置评估
单卡48GB显存服务器配置评估：
- 显存：足以支撑650M参数级模型微调与小规模结构模型
- 内存：16GB不足以稳定处理全量数据与特征缓存，存在OOM风险
- 存储：200GB系统盘不足以同时存放原始数据、环境、模型与多折检查点
- 建议扩容至64GB内存、500GB以上数据盘

### 9.2 资源优化策略
扩容前采用以下策略控制占用：
1. 不全量解压无标注大数据集，按需读取
2. 不全量解压三套结构文件，仅处理带标签子集
3. 预计算表征仅保留池化后向量，不保存中间层
4. 检查点仅保留最佳与最新版本，定期清理
5. 日志聚合写入，减少高频IO
6. 数据处理采用流式分批，避免全量加载

## 10. 风险与备选方案
| 风险项 | 影响 | 应对方案 |
|---|---|---|
| 官方评价聚合方式不明确 | 本地最优与官方排名不一致 | 同时输出多维度结果，保留不同权重的集成版本 |
| 数据同源性超预期 | 验证虚高，线下成绩崩塌 | 采用最严格的连通分量划分，增加负对照校验 |
| 标签标注错误 | 模型学反，性能极差 | 双人交叉审核标签注册表，预留一键反转开关 |
| 弱标签主导训练 | 模型拟合伪标签规律，真实性能下降 | 严格控制弱标签权重，动态采样平衡 |
| 靶标序列大量缺失 | 无法建模匹配交互 | 双路径门控设计，缺失时退化为序列质量评分 |
| 结构数据量少 | 蒸馏过拟合，引入噪声 | 弱蒸馏权重，无增益则关闭该模块 |
| 单卡训练速度慢 | 迭代效率低，错过最优方案 | 先冻结主干快速迭代，砍掉非关键消融 |
| 内存/磁盘不足 | 训练中断，数据损坏 | 关键结果多备份，断点续训 |
| 截止日期误判 | 错过提交 | 内部截止日提前2天，预留缓冲 |

## 11. 预期成果
初赛阶段预期产出：
1. 一套可审计、可追溯的标准化数据集与完整数据治理管线
2. 三套严格无泄漏的数据集划分与验证体系
3. 冻结预训练模型的强基线结果
4. 质量感知的多专家排序主模型
5. 统一支持常规/单域样本的推理接口
6. 可选的LoRA微调、结构蒸馏、检索增强模块
7. 多折交叉验证的百分位集成模型
8. 完全符合官方格式的最终提交文件
9. 完整的算法文档、代码与可复现演示案例

## 12. 开源基线调研总结
### 12.1 现有基线工程进度
当前已有开源项目实现了竞赛基线流程，完成了以下工作：
- 数据预处理：支持多源CSV解析、标签方向统一、百分位转换
- 数据集划分：按研究来源拆分，避免泄漏
- 基线模型：k-mer+理化特征的排序模型，可快速跑通全流程
- 候选生成：基于局部突变的候选生成与排序
- 工程框架：配置化管理、模块化设计、可扩展接口

该基线可作为起步工程框架，在此基础上替换更强模型与优化策略。

### 12.2 可复用公开资源
1. **大规模序列活性基准库**：包含240+数据集，统一格式，可用于补充训练与预训练
2. **三流式序列匹配模型**：开源实现与预训练权重，可直接迁移
3. **图-序列融合排序模型**：结合结构信息的排序模型，可作为教师参考
4. **多模型基准测试框架**：评测了十余种主流模型的排序性能，可作为选型参考